# Este archivo lleva el nombre de __init__ porque de esta forma ayudara a que se interprete que lo
# que este dentro de esta carpeta MyFirstPackage va a ser un paquete que gestionaremos de manera local
# (aunque podria gestionarse de manera remota tambien)

# Guarda el contexto de que esto se termine comportando como un paquete.
# Si no lo tenemos, no es la forma correcta de crear un paquete ya que no podria inicializarse el contexto del
# paquete.